# Audio brainstorm

- Support UI event sounds
- Background music transitions
- New "Sounds" component where entities can specify what their sounds for basic events are like "hit","hitting","alert","death", etc
- Audio director that handles the different listeners for entity events and UI events.